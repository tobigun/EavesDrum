#include "touch.h"

TouchSensorManager touchSensorManager;

void TouchSensorManager::addSensor(DrumPin& pin) {

}

void TouchSensorManager::removeSensor(DrumPin& pin) {

}

bool TouchSensorManager::readSensor(DrumPin& pin) {
  return false;
}
